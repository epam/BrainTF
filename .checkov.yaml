# .checkov.yaml
skip-check:
  - CKV_TF_1            # Temporarily skipped: external modules still use registry source with version, not commit SHA
  - CKV_AWS_300         # Demo only — abort_incomplete_multipart_upload not required in demo scenario
  - CKV2_AWS_5          # Demo only — security group not attached to instance in standalone demo resource
  - CKV2_AWS_62         # Demo only — S3 event notifications not required in demo scenario
  - CKV_AWS_144         # Demo only — cross-region replication not required in demo scenario
  - CKV2_AWS_61         # Demo only — lifecycle configuration present but simplified for demo
  - CKV_AWS_18          # Demo only — access logging configured to same bucket (self-logging) in demo
  - CKV2_AWS_64         # Demo only — KMS key policy not required in demo scenario

output: cli
quiet: true
